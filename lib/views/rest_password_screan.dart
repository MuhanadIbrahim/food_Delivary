import 'package:flutter/material.dart';

import '../widgets/restpassword_body.dart';

class RestPasswordScrean extends StatelessWidget {
  const RestPasswordScrean({super.key});

  @override
  Widget build(BuildContext context) {
    return const RestPasswordBody();
  }
}

